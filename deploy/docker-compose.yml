services:
  odyssey-watch:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    container_name: odyssey-watch
    restart: unless-stopped
    environment:
      - NTFY_TOPIC=${NTFY_TOPIC}
      - TZ=Europe/Prague
    volumes:
      - ./data:/data
